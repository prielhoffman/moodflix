-- Create the MoodFlix database.
-- Run from project root: psql -U postgres -h 127.0.0.1 -f scripts/create_moodflix_db.sql
-- If moodflix already exists, you will see an error; that is safe to ignore.

CREATE DATABASE moodflix;
