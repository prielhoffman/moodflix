# -------------------- Database (backend + Alembic + Docker) --------------------
# Use host "localhost" when running backend on your machine.
# Use host "db" when backend runs inside Docker Compose network.
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=moodflix
POSTGRES_HOST=localhost
POSTGRES_PORT=5432

# Optional direct SQLAlchemy connection string. If set, backend prefers this.
# Example: postgresql://postgres:postgres@localhost:5432/moodflix
DATABASE_URL=

# -------------------- Auth / JWT --------------------
# Replace with a long random value in any shared or production environment.
SECRET_KEY=change-me-in-production
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Optional environment name used for production safety warnings.
ENV=development
APP_ENV=development

# -------------------- TMDB (optional enrichment) --------------------
# Leave empty to run without TMDB enrichment.
TMDB_API_KEY=

# TMDB cache / enrichment tuning
TMDB_CACHE_TTL_SECONDS=21600
TMDB_CACHE_MAX_SIZE=1024
TMDB_NEGATIVE_CACHE_TTL_SECONDS=120
TMDB_ENRICH_MAX_WORKERS=5

# TMDB ingestion script pages (scripts/ingest_tmdb.py)
TMDB_PAGES=2

# -------------------- Frontend (optional) --------------------
# API base for frontend dev server.
VITE_API_BASE_URL=http://127.0.0.1:8000